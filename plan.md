# Pomodoro Timer Implementation Plan

## 1. 目的

このドキュメントは、ポモドーロタイマー Web アプリケーションを段階的に実装していくための計画を整理したものです。

添付の UI モック、[architecture.md](architecture.md)、[features.md](features.md) を前提に、実装の順序と粒度を明確にすることを目的とします。

## 2. 実装方針

実装は、1 回ごとに動く成果物が増える粒度で進めます。

- 1 フェーズは、利用者が触って価値を感じられる単位で区切る
- 1 タスクは、1 つの責務に閉じる単位で区切る
- 1 タスクの完了条件には、実装、UI 接続、最低限のテスト確認を含める

タイマー系のアプリケーションは、見た目だけを先に広げるより、縦に薄く完成させる進め方の方が安定しやすいです。

## 3. 推奨するタスク粒度

### 3.1 フェーズの粒度

1 フェーズは、利用者目線で 1 つの利用シナリオが成立する大きさにする。

例:

- 画面が表示される
- タイマーを開始できる
- タイマーを停止できる
- 設定を保存できる
- 終了通知を受け取れる

### 3.2 タスクの粒度

1 タスクは、1 つの責務に閉じる大きさにする。

例:

- 残り時間計算関数を作る
- 開始ボタンから state を更新する
- 設定値を localStorage に保存する
- 通知音を鳴らす adapter を実装する

### 3.3 完了条件の粒度

各タスクは、次の条件を満たして完了とする。

- 実装が存在する
- UI または呼び出し元に接続されている
- 最低限のテストまたは動作確認がある

## 4. 段階的な実装計画

### Phase 1: 土台構築

目的:

- Flask とフロントエンドの基本構成を整える
- UI モックに近い静的画面を表示できるようにする
- JavaScript を責務ごとに分割する

実装対象:

- Flask の app factory 化
- トップページ配信
- テンプレート配置
- 静的ファイル配信
- index.html の骨組み作成
- style.css のベーススタイル作成
- main.js、domain、ui、ports のファイル分割

完了条件:

- ブラウザでトップページが表示される
- UI モックに近い静的レイアウトが見える
- 主要ファイルの責務が分離されている

### Phase 2: タイマー MVP

目的:

- まずは 1 つのタイマーとして正しく使える状態にする

実装対象:

- 作業モードのタイマー表示
- 開始
- 一時停止
- リセット
- 残り時間の mm:ss 表示
- 現在モード表示
- targetEpochMs を用いた残り時間再計算

完了条件:

- 利用者が作業タイマーを開始、停止、リセットできる
- タブ復帰後も残り時間が大きくずれない
- 時間計算ロジックに最低限のテストがある

### Phase 3: モード切替とポモドーロ基本ルール

目的:

- 単なるタイマーではなく、ポモドーロとして一巡できる状態にする

実装対象:

- 短休憩モード
- 長休憩モード
- モード切替
- 作業完了回数カウント
- 一定回数ごとの長休憩判定
- 作業完了後の短休憩遷移
- 長休憩後の作業復帰
- 周回数表示

完了条件:

- 作業と休憩をポモドーロのルールで一巡できる
- 状態遷移ロジックが純粋関数として分離されている
- モード遷移に対するテストがある

### Phase 4: 設定と保存

目的:

- 利用者ごとに時間設定を変更でき、再訪問時も復元される状態にする

実装対象:

- 作業時間設定
- 短休憩時間設定
- 長休憩時間設定
- 長休憩までの回数設定
- 設定値バリデーション
- localStorage 保存
- ページ再読込時の復元
- ブラウザ再訪問時の復元

完了条件:

- 設定変更後に次回起動でも内容が維持される
- 不正値入力時に適切なエラー表示が出る
- storage adapter に対するテストまたは動作確認がある

### Phase 5: 通知と実用性向上

目的:

- セッション終了を見逃しにくい状態にする

実装対象:

- 通知音
- ブラウザ通知
- 通知の ON/OFF
- 音や通知の設定
- モード切替時の視覚フィードバック

完了条件:

- セッション終了時に利用者へ明確な通知が行われる
- 通知の有無を設定で制御できる

### Phase 6: UI 仕上げ

目的:

- 見た目と操作性の完成度を上げる

実装対象:

- UI モックへの寄せ込み
- モバイル対応
- キーボード操作対応
- アクセシビリティ対応
- エラー表示の整理
- レイアウト崩れ防止

完了条件:

- デスクトップとモバイルで破綻なく表示できる
- 主要操作が分かりやすい
- 基本的なアクセシビリティに配慮されている

### Phase 7: 履歴とサーバ拡張

目的:

- 必要に応じて永続化と集計の機能を追加する

実装対象:

- Flask の設定 API
- Flask の履歴 API
- セッション記録 API
- SQLite 保存
- セッション履歴表示
- 日別集計

完了条件:

- 完了したセッションを永続化できる
- 履歴や集計を画面上で参照できる

## 5. 各フェーズで切るべきタスク単位

### 5.1 Domain 単位

次のように、関数単位でタスクを切る。

- 初期 state 生成
- 残り時間計算
- 開始処理
- 一時停止処理
- リセット処理
- モード遷移処理
- 長休憩判定処理
- 設定バリデーション

### 5.2 UI 接続単位

イベント単位でタスクを切る。

- 開始ボタン接続
- 一時停止ボタン接続
- リセットボタン接続
- モード切替ボタン接続
- 設定フォーム接続

### 5.3 保存処理単位

adapter 単位でタスクを切る。

- state 保存
- state 復元
- 設定保存
- 実行中タイマー復元

### 5.4 テスト単位

モジュール単位で積み上げる。

- 時間計算テスト
- 状態遷移テスト
- 設定バリデーションテスト
- Flask ルートテスト
- 保存層テスト

## 6. 実装順の推奨

より具体的には、次の順で進めると安全です。

1. 画面骨組みと静的 UI
2. timeMath と timerState
3. 開始、一時停止、リセット
4. render と handlers の接続
5. focus、shortBreak、longBreak の切替
6. completedFocusCount と長休憩判定
7. settings と validation
8. storage による保存と復元
9. notifier
10. UI 調整とレスポンシブ対応
11. 必要なら Flask API と履歴保存

## 7. この計画のポイント

この実装計画では、最初にすべての機能を薄く作るのではなく、使える一本の流れを順に完成させていくことを重視します。

特に重要なのは次の点です。

- フェーズ 2 でタイマーの根幹を固める
- フェーズ 3 でポモドーロとしてのルールを完成させる
- フェーズ 4 で保存と復元を安定させる

この 3 段階が固まれば、その後の通知、UI 改善、履歴保存は比較的安全に拡張できます。