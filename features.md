# Pomodoro Timer Features

## 1. 目的

このドキュメントは、ポモドーロタイマー Web アプリケーションにおいて実装対象となる機能を整理したものです。

機能の優先度を明確にし、MVP から段階的に開発できるようにすることを目的とします。

## 2. 必須機能

まずはアプリとして成立するための最小構成を実装します。

- 作業モードのタイマー表示
- 短休憩モードのタイマー表示
- 長休憩モードのタイマー表示
- 開始
- 一時停止
- リセット
- モード切替
- 残り時間の mm:ss 表示
- 現在モードの表示
- targetEpochMs を用いた残り時間再計算
- 初期設定値での起動

## 3. ポモドーロ固有の機能

単なるカウントダウンではなく、ポモドーロ運用として必要な機能です。

- 作業完了回数のカウント
- 一定回数ごとの長休憩判定
- 作業完了後に短休憩へ遷移するルール
- 長休憩後に作業へ戻るルール
- 現在の周回数表示
- 次に長休憩が入る条件の管理

## 4. 設定機能

利用者が運用スタイルを変更できるようにします。

- 作業時間の設定
- 短休憩時間の設定
- 長休憩時間の設定
- 長休憩までの回数設定
- 設定値バリデーション
- 不正値入力時のエラー表示

## 5. 保存と復元

ページ再読込や再訪問でも状態を引き継げるようにします。

- 設定値の保存
- 最後の state の保存
- ページ再読込時の state 復元
- ブラウザ再訪問時の設定復元
- 実行中タイマーの復元方針の実装

実行中タイマーの復元では、終了予定時刻を保存し、復元時に残り時間を再計算する方針を採用します。

## 6. 通知機能

セッション区切りを利用者が認識しやすくするための機能です。

- セッション終了時の通知音
- ブラウザ通知
- 通知の ON/OFF
- 音や通知可否の設定
- モード切替時の視覚フィードバック

## 7. UI/UX 機能

モック再現だけでなく、実用性と使いやすさを担保するための機能です。

- 主要操作ボタンの視認性確保
- モードごとの見た目の切替
- 現在状態の分かりやすい表示
- モバイル対応
- キーボード操作対応
- アクセシビリティ対応
- 軽快な画面更新
- レイアウト崩れ防止
- エラー表示の整備

## 8. Flask 側の機能

初期段階では薄い Web 層として必要最小限を実装します。

- トップページ配信
- テンプレート返却
- 静的ファイル配信
- 将来の設定 API
- 将来の履歴 API
- 将来のセッション記録 API

## 9. 将来拡張候補

初期実装の対象外ですが、後から追加しやすいように設計しておく機能です。

- セッション履歴表示
- 日別集計
- 完了回数の可視化
- 今日の集中時間表示
- 自動開始設定
- テーマ切替
- タスク名入力
- タスクごとのポモドーロ記録
- SQLite 保存
- 複数端末同期

## 10. テストしやすさのために分離して実装する対象

機能本体とあわせて、次の単位でモジュールを分けて実装します。

- 残り時間計算関数
- 秒数フォーマット関数
- 状態遷移関数
- 設定バリデーション関数
- localStorage adapter
- clock adapter
- Flask の route
- 将来の repository 層

## 11. 実装優先順位

実装は次の順序で進める想定です。

1. タイマー表示、開始、一時停止、リセット、モード切替
2. 正しい時間計算と state 管理
3. 設定変更と保存
4. 作業回数カウントと長休憩ルール
5. 通知
6. 履歴や集計