# Pomodoro Timer Web App Architecture

## 1. 目的

このドキュメントは、Flask と HTML/CSS/JavaScript を用いて実装するポモドーロタイマー Web アプリのアーキテクチャ案を整理したものです。

今回の方針では、次の 3 点を重視します。

- UI モックに沿って実装しやすいこと
- タイマーの挙動が安定していること
- ユニットテストしやすいこと

## 2. 全体方針

### 2.1 サーバは薄く、タイマー本体はクライアント側に置く

タイマーの時間進行そのものはブラウザ側で管理し、Flask は画面配信と将来の API 提供に徹します。

この方針を採る理由は次の通りです。

- 秒単位の表示更新をサーバ側に寄せると複雑になりやすい
- 通信遅延や再読込の影響を受けにくい
- タイマーの中核ロジックを JavaScript の純粋関数として分離しやすい

### 2.2 1 ページ構成で開始し、必要に応じて拡張する

初期段階は 1 ページのシングルビュー構成とし、必要な機能を段階的に追加します。

初期段階の中心機能は次の通りです。

- 作業時間、短休憩、長休憩の切替
- 開始、一時停止、リセット
- 現在モードと残り時間の表示
- 設定値と状態のローカル保存

## 3. アーキテクチャ概要

### 3.1 レイヤ構成

アプリケーションは大きく次の 4 層に分けます。

1. Web 層
   Flask が担当する層。HTML の返却、静的ファイル配信、将来の API 提供を行う。

2. フロントエンド UI 層
   HTML と CSS、および DOM 操作を行う JavaScript を配置する層。表示とイベント処理に責務を限定する。

3. フロントエンド Domain 層
   タイマー状態、時間計算、状態遷移などの中核ロジックを置く層。ここは純粋関数中心に設計する。

4. 永続化層
   初期段階では localStorage、将来拡張時は SQLite などを利用する。保存方式は adapter で吸収する。

### 3.2 データの流れ

1. Flask がトップページを返す
2. ブラウザで JavaScript が初期 state を生成または復元する
3. ユーザー操作を UI 層が受け取る
4. Domain 層が次の state を返す
5. UI 層が画面を再描画する
6. 保存対象 state を storage adapter 経由で永続化する

## 4. 推奨ディレクトリ構成

実装対象は 1.pomodoro 配下に配置する。

```text
1.pomodoro/
  app.py
  requirements.txt
  pomodoro_app/
    __init__.py
    config.py
    routes.py
    services/
      session_service.py
    repositories/
      session_repository.py
  templates/
    index.html
  static/
    css/
      style.css
    js/
      main.js
      domain/
        timerState.js
        timerReducer.js
        timeMath.js
        settings.js
      ports/
        clock.js
        storage.js
        notifier.js
      ui/
        render.js
        handlers.js
  tests/
    python/
      test_routes.py
      test_session_repository.py
    js/
      timeMath.test.js
      timerReducer.test.js
```

## 5. 各責務の設計

### 5.1 Flask 側

Flask は薄い入口とする。

役割は次の通り。

- 画面の返却
- 静的ファイルの配信
- 将来的な設定保存 API と履歴 API の提供

初期段階のルートは最小限でよい。

- GET /

将来追加する可能性が高い API。

- GET /api/settings
- POST /api/settings
- GET /api/history
- POST /api/sessions

### 5.2 UI 層

UI 層は次の責務に限定する。

- DOM 要素の取得
- ボタンやフォームのイベント登録
- state を受け取って画面を描画

UI 層では時間計算やモード遷移ルールを持たない。

### 5.3 Domain 層

Domain 層はポモドーロの中核ロジックを担当する。

主な責務は次の通り。

- 初期 state の生成
- 残り時間の計算
- 作業、短休憩、長休憩のモード管理
- 開始、一時停止、リセットの状態遷移
- 1 セッション完了後の次モード決定
- 設定値のバリデーション

ここは可能な限り純粋関数で構成する。

### 5.4 Ports 層

副作用を持つ処理は Domain から切り離し、ports として抽象化する。

対象は次の通り。

- clock: 現在時刻の取得
- storage: localStorage への保存と復元
- notifier: 通知や音の再生

これにより、テストでは偽物の実装へ差し替えられる。

## 6. タイマー状態の考え方

タイマー state は単一オブジェクトで管理する。

最低限必要な項目は次の通り。

- mode
- isRunning
- remainingSeconds
- targetEpochMs
- completedFocusCount
- settings

ここで重要なのは、毎秒 1 ずつ減算するのではなく、現在時刻と targetEpochMs の差分から remainingSeconds を求めることです。

この設計により、次の問題を避けやすくなります。

- タブが非アクティブな間に setInterval が遅延する問題
- 再描画後に表示がずれる問題
- テストで時間依存の不安定さが出る問題

## 7. テストしやすさを高めるための設計原則

### 7.1 状態遷移を純粋関数に閉じ込める

開始、一時停止、リセット、モード切替は reducer 風の関数群として実装する。

これにより、入力 state と action に対して期待 state を直接テストできる。

### 7.2 時刻取得を抽象化する

Date.now を直接各所で呼ばず、clock adapter を経由させる。

テストでは固定時刻を返す fake clock を利用する。

### 7.3 保存処理を直接書かない

localStorage を UI や Domain から直接呼ばず、storage adapter に集約する。

### 7.4 表示フォーマットを分離する

秒数から mm:ss を作る関数など、表示用変換も純粋関数として切り出す。

### 7.5 Flask は app factory 形式にする

アプリ生成を create_app に集約し、テスト時の設定差し替えを容易にする。

### 7.6 保存層は repository 経由にする

履歴保存を導入する段階では、route から直接 SQL を叩かず repository に閉じ込める。

## 8. テスト戦略

### 8.1 JavaScript ユニットテスト

最重要。対象は次の通り。

- timeMath.js
- timerReducer.js
- settings.js

確認内容は次の通り。

- 秒数表示フォーマット
- 残り時間計算
- 開始、一時停止、リセットの挙動
- モード切替
- 設定値の妥当性

### 8.2 JavaScript UI テスト

必要最小限にとどめる。

- ボタン押下で適切な action が発火するか
- 描画関数で表示が更新されるか

### 8.3 Flask テスト

- トップページが返ること
- 将来追加する API の正常系、異常系

### 8.4 保存層テスト

- localStorage adapter の保存と復元
- 将来の SQLite repository の読み書き

## 9. 段階的な実装計画

## 9. 実装機能一覧

この章では、ポモドーロタイマーアプリとして実装対象になる機能を優先度別に整理する。

### 9.1 必須機能

まずはアプリとして成立するための最小構成を実装する。

- 作業モードのタイマー表示
- 短休憩モードのタイマー表示
- 長休憩モードのタイマー表示
- 開始
- 一時停止
- リセット
- モード切替
- 残り時間の mm:ss 表示
- 現在モードの表示
- targetEpochMs を用いた残り時間再計算
- 初期設定値での起動

### 9.2 ポモドーロ固有の機能

単純なカウントダウンではなく、ポモドーロ運用として必要な機能を実装する。

- 作業完了回数のカウント
- 一定回数ごとの長休憩判定
- 作業完了後に短休憩へ遷移するルール
- 長休憩後に作業へ戻るルール
- 現在の周回数表示
- 次に長休憩が入る条件の管理

### 9.3 設定機能

利用者が運用スタイルを変更できるようにする。

- 作業時間の設定
- 短休憩時間の設定
- 長休憩時間の設定
- 長休憩までの回数設定
- 設定値バリデーション
- 不正値入力時のエラー表示

### 9.4 保存と復元

ページ再読込や再訪問でも状態を引き継げるようにする。

- 設定値の保存
- 最後の state の保存
- ページ再読込時の state 復元
- ブラウザ再訪問時の設定復元
- 実行中タイマーの復元方針の実装

実行中タイマーの復元では、終了予定時刻を保存し、復元時に残り時間を再計算する方針を採る。

### 9.5 通知機能

セッション区切りを利用者が認識しやすくする。

- セッション終了時の通知音
- ブラウザ通知
- 通知の ON/OFF
- 音や通知可否の設定
- モード切替時の視覚フィードバック

### 9.6 UI/UX 機能

モック再現だけでなく、実用性と使いやすさを担保する。

- 主要操作ボタンの視認性確保
- モードごとの見た目の切替
- 現在状態の分かりやすい表示
- モバイル対応
- キーボード操作対応
- アクセシビリティ対応
- 軽快な画面更新
- レイアウト崩れ防止
- エラー表示の整備

### 9.7 Flask 側の機能

初期段階では薄い Web 層として必要最小限を実装する。

- トップページ配信
- テンプレート返却
- 静的ファイル配信
- 将来の設定 API
- 将来の履歴 API
- 将来のセッション記録 API

### 9.8 将来拡張候補

初期実装の対象外だが、後から追加しやすいように設計しておく。

- セッション履歴表示
- 日別集計
- 完了回数の可視化
- 今日の集中時間表示
- 自動開始設定
- テーマ切替
- タスク名入力
- タスクごとのポモドーロ記録
- SQLite 保存
- 複数端末同期

### 9.9 テストしやすさのために分離して実装する対象

機能本体とあわせて、次の単位でモジュールを分けて実装する。

- 残り時間計算関数
- 秒数フォーマット関数
- 状態遷移関数
- 設定バリデーション関数
- localStorage adapter
- clock adapter
- Flask の route
- 将来の repository 層

### 9.10 実装優先順位

実装は次の順序で進めるのが妥当である。

1. タイマー表示、開始、一時停止、リセット、モード切替
2. 正しい時間計算と state 管理
3. 設定変更と保存
4. 作業回数カウントと長休憩ルール
5. 通知
6. 履歴や集計

## 10. 段階的な実装計画

### Phase 1: 土台作成

- Flask の app factory 化
- index.html と style.css の配置
- JavaScript モジュール分割

### Phase 2: タイマー MVP

- 開始、一時停止、リセット
- モード切替
- 残り時間表示
- localStorage 復元

### Phase 3: ポモドーロルール拡張

- 長休憩までのカウント
- 自動遷移の制御
- 通知音やブラウザ通知

### Phase 4: 永続化拡張

- 設定保存 API
- セッション履歴 API
- SQLite 導入

### Phase 5: 品質向上

- テスト拡充
- UI 調整
- レスポンシブ強化
- アクセシビリティ改善

## 11. 主要なトレードオフ

### 10.1 タイマーをクライアントに置く利点

- 実装がシンプル
- オフラインでも動きやすい
- テストの対象を分離しやすい

### 10.2 タイマーをクライアントに置く注意点

- 複数端末で同一 state を同期する用途には向かない
- サーバ側で厳密な進行管理はしない

現時点の用途では、このトレードオフは妥当である。

## 12. 現時点の結論

本アプリでは、Flask を薄い Web 層として使い、タイマーの中核ロジックをブラウザ側の Domain 層に集約する構成を採用する。

特に、ユニットテストのしやすさを優先して、次の原則を維持する。

- 時間計算は純粋関数にする
- 状態遷移は UI から分離する
- 副作用は ports に閉じ込める
- Flask は入口に徹する
- 永続化方式は adapter と repository で隠蔽する

この構成により、UI モックに合わせた実装と、安定したタイマー挙動、テスト容易性の 3 点を両立しやすくなる。